{
  "name": "riku-hub",
  "version": "0.1.1",
  "private": true,
  "packageManager": "pnpm@10.6.0",
  "scripts": {
    "dev:worker": "wrangler dev --config wrangler.toml",
    "dev:web": "corepack pnpm --filter @riku-hub/web dev",
    "build": "corepack pnpm -r run build",
    "build:web": "corepack pnpm --filter @riku-hub/web build",
    "deploy": "corepack pnpm check && corepack pnpm test && corepack pnpm build:web && pnpm wrangler deploy",
    "check": "corepack pnpm -r run check",
    "test": "corepack pnpm -r run test",
    "check-deps": "madge --circular packages/worker/src packages/shared/src"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250312.0",
    "madge": "^8.0.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.7",
    "wrangler": "^4.4.0"
  }
}
